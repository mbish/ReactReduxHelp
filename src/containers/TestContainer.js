import { connect } from 'react-redux'
import Test from '../components/Test.js'
import _ from 'underscore';

const mapStateToProps = state => {
  return {
  }
}

const mapDispatchToProps = dispatch => {
  return {
  }
}

export const connectedTest = connect(
  mapStateToProps,
  mapDispatchToProps,
)(Test)
